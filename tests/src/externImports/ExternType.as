/**
 * Test case: class declaration for externImports.ExternType (should be kept as-is).
 */
package externImports {
    public class ExternType {
        public function ExternType() {
        }
    }
}
